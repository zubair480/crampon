# PIN REQUIRED. jax 0.11 removed jax.device_put_replicated, which brax 0.14.2
# still calls in ppo/train.py:756 -- a fresh `pip install playground` resolves
# to 0.11 and every training run dies instantly with no traceback.
# 0.9.2 is the newest jax that works.
jax<0.10
jaxlib<0.10
brax>=0.14.2
playground==0.2.0
huggingface_hub[cli]

# GPU machines: replace the jax line above with
#   jax[cuda12]<0.10
